@model List<WebMVC_NetCore.Models.BookModels>
@{
    var dateNow = DateTime.Now.ToString("dd/MM/yyyy");
    switch (1)
    {
        case 1: break;
    }

    if (1 > 0)
    {

    }
    else
    {
        
    }

    var listStr = new List<string>();
    for (int i = 0; i < 10; i++)
    {
        listStr.Add(i.ToString());
    }
}
<p>Ngày hiện tại : @dateNow</p>

@foreach (var item in listStr)
{
    <p>@item</p>
}

<p>Đây là dữ liệu được lấy từu AJAX 111 </p>
<table class="table">
    <thead>
        <tr>
            <th>BookId</th>
            <th>BookName</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.BookID</td>
                    <td>@item.BookName</td>
                </tr>
            }
        }
    </tbody>
</table>
